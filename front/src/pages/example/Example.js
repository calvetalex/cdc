// React Core
import React, { Component } from 'react';

export default class Example extends Component {
    render() {
        return (
            <div>
                <h1>An example page with lots of contents (spoiler: no).</h1>
            </div>
        );
    }
}
